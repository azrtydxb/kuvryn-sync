# The HealthCheck validating webhook needs a TLS certificate; cert-manager
# issues it and injects the CA bundle. cert-manager must be installed first.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kuvryn-sync.fullname" . }}-webhook
  labels:
    app.kubernetes.io/name: {{ include "kuvryn-sync.name" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "kuvryn-sync.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: webhook
      port: 443
      targetPort: 9443
---
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "kuvryn-sync.fullname" . }}-selfsigned
  labels:
    app.kubernetes.io/name: {{ include "kuvryn-sync.name" . }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kuvryn-sync.fullname" . }}-webhook
  labels:
    app.kubernetes.io/name: {{ include "kuvryn-sync.name" . }}
spec:
  dnsNames:
    - {{ include "kuvryn-sync.fullname" . }}-webhook.{{ .Release.Namespace }}.svc
    - {{ include "kuvryn-sync.fullname" . }}-webhook.{{ .Release.Namespace }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "kuvryn-sync.fullname" . }}-selfsigned
  secretName: {{ include "kuvryn-sync.fullname" . }}-webhook-cert
---
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "kuvryn-sync.fullname" . }}-validating
  labels:
    app.kubernetes.io/name: {{ include "kuvryn-sync.name" . }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kuvryn-sync.fullname" . }}-webhook
webhooks:
  - name: vhealthcheck-v1alpha1.kb.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: Fail
    clientConfig:
      service:
        name: {{ include "kuvryn-sync.fullname" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: /validate-sync-kuvryn-io-v1alpha1-healthcheck
    rules:
      - apiGroups: ["sync.kuvryn.io"]
        apiVersions: ["v1alpha1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["healthchecks"]
---
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ include "kuvryn-sync.fullname" . }}-mutating
  labels:
    app.kubernetes.io/name: {{ include "kuvryn-sync.name" . }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "kuvryn-sync.fullname" . }}-webhook
webhooks:
  # Records who approved an Application's plan, and which plan digest.
  - name: mapplication-v1alpha1.kb.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: Fail
    clientConfig:
      service:
        name: {{ include "kuvryn-sync.fullname" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: /mutate-sync-kuvryn-io-v1alpha1-application
    rules:
      - apiGroups: ["sync.kuvryn.io"]
        apiVersions: ["v1alpha1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["applications"]
