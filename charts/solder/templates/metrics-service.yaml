apiVersion: v1
kind: Service
metadata:
  name: {{ include "solder.fullname" . }}-metrics
  labels:
    app.kubernetes.io/name: {{ include "solder.name" . }}
spec:
  selector:
    app.kubernetes.io/name: {{ include "solder.name" . }}
  ports:
    - name: https
      port: 8443
      targetPort: 8443
