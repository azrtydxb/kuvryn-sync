---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: notificationsinks.solder.io
spec:
  group: solder.io
  names:
    kind: NotificationSink
    listKind: NotificationSinkList
    plural: notificationsinks
    singular: notificationsink
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .spec.type
          name: Type
          type: string
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1alpha1
      schema:
        openAPIV3Schema:
          description: NotificationSink is a destination for Application lifecycle notifications.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: spec defines the sink.
              properties:
                secretRef:
                  description: |-
                    secretRef names a Secret in the sink's namespace holding `url` (an
                    https URL) and, for webhook sinks, `hmacKey` used to sign each body.
                  properties:
                    name:
                      description: name is the Secret name.
                      minLength: 1
                      type: string
                  required:
                    - name
                  type: object
                type:
                  description: type selects the delivery format.
                  enum:
                    - webhook
                    - slack
                  type: string
              required:
                - secretRef
                - type
              type: object
          required:
            - spec
          type: object
      served: true
      storage: true
      subresources: {}
